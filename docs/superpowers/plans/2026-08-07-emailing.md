# Emailing Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ship `/admin/emailing` with Resend send/logs/sequences/metrics, delayed jobs via cron, and webhook status updates.

**Architecture:** Supabase tables + `lib/emailing/*` + admin API routes + cron + Resend webhook. UI client dashboard with 4 in-page tabs matching Botio chrome.

**Tech stack:** Next.js 14 App Router, Supabase admin client, Resend, Tailwind, existing Button/Card/Badge/Input.

## File map

| File | Responsibility |
|------|----------------|
| `supabase/migrations/0054_emailing.sql` | Tables + RLS + seed sequences |
| `lib/emailing/types.ts` | Shared types |
| `lib/emailing/templates.ts` | Placeholder HTML helpers |
| `lib/emailing/send.ts` | Resend send + log insert |
| `lib/emailing/queries.ts` | DB reads/writes for UI APIs |
| `lib/emailing/jobs.ts` | Cron job processing |
| `lib/email-tags.ts` | `triggerEmailSequence` |
| `app/api/admin/emailing/**` | Admin APIs |
| `app/api/emailing/webhook/route.ts` | Resend events |
| `app/api/cron/emailing-jobs/route.ts` | Process due jobs |
| `app/admin/emailing/**` | Page + dashboard UI |
| `components/admin/admin-header.tsx` | Nav item |
| `vercel.json` | Hourly cron |

## Tasks

1. Migration + apply remotely
2. Core lib (types, templates, send, queries, jobs, tags)
3. API routes + cron + webhook
4. Admin UI + nav
5. Install `resend`, wire vercel cron
6. Lint/typecheck smoke + commit

## Self-review

- No Prisma; paths under `/admin` and `/api/admin`
- Edit scope: active + delay only
- Env vars documented in design spec
